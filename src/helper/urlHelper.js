export const handleRoleDashboard = (url) => {
    if (url === '/usr/dashboardOpsLead') return '/dashboard-ops-lead'
    if (url === '/usr/dashboardKU') return '/dashboard'
    if (url === '/usr/dashboardWHTeam') return '/dashboard-wh'
};